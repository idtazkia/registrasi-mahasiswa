CREATE TABLE smart_test (
  id                VARCHAR (36),
  nama              VARCHAR(255) NOT NULL,
  sekolah           VARCHAR(255),
  no_hp             VARCHAR(255),
  nilai             VARCHAR(255),
  PRIMARY KEY (id)
);